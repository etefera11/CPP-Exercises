#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;

int main()
{
    double length, width, height, l, w, h;

    cout << "Enter the length of the Candy Bar: ";
    cin >> length;

    cout << "Enter the width of the Candy Bar: ";
    cin >> width;

    cout << "Enter the height of the Candy Bar: ";
    cin >> height;

    cout << fixed << showpoint << setprecision(2);

    le
}
